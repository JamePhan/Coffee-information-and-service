export interface ICustomer {
  customerId: number;
  name: string;
  phone: string;
  address: string;
  email: string;
  accountId: number;
}

export interface ICustomerbanned {
  profileId: number;
  role: string;
}